import { Alert, AlertDescription, AlertTitle } from '@/components/ui/alert'

export default function AlertBasicExample() {
  return (
    <Alert><AlertTitle>Basic</AlertTitle><AlertDescription>This alert is rendered by Fict.</AlertDescription></Alert>
  )
}
