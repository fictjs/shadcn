import { Checkbox } from '@/components/ui/checkbox'

export default function CheckboxDisabledExample() {
  return (
    <label class="flex items-center gap-2"><Checkbox disabled defaultChecked /> Disabled</label>
  )
}
