import { Checkbox } from '@/components/ui/checkbox'

export default function CheckboxGroupExample() {
  return (
    <label class="flex items-center gap-2"><Checkbox defaultChecked /> Group</label>
  )
}
