import { Checkbox } from '@/components/ui/checkbox'

export default function CheckboxDemoExample() {
  return (
    <label class="flex items-center gap-2"><Checkbox defaultChecked /> Demo</label>
  )
}
